import React from 'react';

interface ResourceEditorProps {
  resources: unknown;
}

const ResourceEditor = (props: ResourceEditorProps) => {
  return (
    <div>
            
    </div>
  );
};

export default ResourceEditor;
